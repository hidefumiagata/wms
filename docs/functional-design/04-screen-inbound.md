# 機能設計書 — 画面設計 入荷管理（INB-002〜006）

> INB-001（入荷予定一覧）は `03-screen-inbound-list.md` に別途記述済み。

---

## INB-002 入荷予定登録

### 1. 画面概要

| 項目 | 内容 |
|------|------|
| **画面ID** | `INB-002` |
| **画面名** | 入荷予定登録 |
| **URL パス** | `/inbound/slips/new` |
| **対象ロール** | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| **概要** | 通常入荷の入荷予定を手動で登録する画面。入荷予定日・仕入先などのヘッダー情報と、商品・予定数量の明細を入力して保存する。登録時のステータスは「入荷予定」で固定される。 |

---

### 2. 画面レイアウト

**モックアップ**: [INB-002-inbound-new.html](mockups/INB-002-inbound-new.html)

```
┌─────────────────────────────────────────────────────────────┐
│ ヘッダー（WMS / 倉庫切替 / ユーザー名 / ログアウト）         │
├──────────┬──────────────────────────────────────────────────┤
│ サイドバー│ ホーム › 入荷管理 › 入荷予定登録                │
│ 入荷管理  │────────────────────────────────────────────────│
│  入荷予定 │ 入荷予定登録                                    │
│  一覧    │                                                  │
│  入荷実績 │ ┌─────────────────────────────────────────────┐ │
│  照会    │ │ ヘッダー情報                                 │ │
│          │ │  入荷予定日 *  [____/____/____]               │ │
│          │ │  種別         [通常入荷（固定）]              │ │
│          │ │  仕入先 *     [セレクト▼___________]         │ │
│          │ │  備考         [テキストエリア________________] │ │
│          │ └─────────────────────────────────────────────┘ │
│          │ ┌─────────────────────────────────────────────┐ │
│          │ │ 入荷明細  [＋ 行追加]                       │ │
│          │ │ No │ 商品コード │ 商品名  │ 予定数量 │ 削除  │ │
│          │ │  1 │ [入力]     │ [自動] │ [数値]  │ [×]  │ │
│          │ │  2 │ [入力]     │ [自動] │ [数値]  │ [×]  │ │
│          │ └─────────────────────────────────────────────┘ │
│          │                      [キャンセル]  [登録する]    │
└──────────┴──────────────────────────────────────────────────┘
```

コンテンツエリアの構成:
1. **ヘッダー情報カード**: 入荷予定日（必須）・種別（表示のみ「通常入荷」）・仕入先（必須・セレクト）・備考（任意・textarea）
2. **入荷明細カード**: 商品コード（必須・入力後に商品名を自動補完）・予定数量（必須・数値）の行追加テーブル。最低1行必須
3. **フッターボタン**: 右寄せに「キャンセル」「登録する」

---

### 3. 画面項目一覧

| 項目ID | 項目名 | 種別 | 必須 | 初期値 | バリデーション | 備考 |
|--------|--------|------|:----:|--------|--------------|------|
| INB002-H01 | 入荷予定日 | 日付 | ○ | 現在営業日 | 必須・現在営業日以降 | el-date-picker |
| INB002-H02 | 種別 | 表示 | — | 通常入荷 | — | 変更不可。手動登録は常に「通常入荷」 |
| INB002-H03 | 仕入先 | セレクト | ○ | — | 必須 | 有効な仕入先取引先マスタから選択 |
| INB002-H04 | 備考 | テキストエリア | — | — | 最大500文字 | |
| INB002-D01 | 明細No | 表示 | — | 自動採番 | — | 行追加時に連番自動付与 |
| INB002-D02 | 商品コード | テキスト | ○ | — | 必須・商品マスタ存在チェック | 入力後に商品名を自動補完 |
| INB002-D03 | 商品名 | 表示 | — | — | — | 商品コード入力後に自動表示（読み取り専用） |
| INB002-D04 | 予定数量 | 数値 | ○ | — | 必須・1以上999999以下の整数 | |
| INB002-D05 | 削除ボタン | ボタン | — | — | — | 明細が1行のみの場合は非活性 |
| INB002-BTN01 | 行追加ボタン | ボタン | — | — | — | クリックで明細テーブル末尾に空行追加 |
| INB002-BTN02 | キャンセルボタン | ボタン | — | — | — | 確認ダイアログ後にINB-001へ遷移 |
| INB002-BTN03 | 登録するボタン | ボタン | — | — | — | バリデーション後にPOST /api/v1/inbound/slips |

---

### 4. イベント一覧

| イベントID | トリガー | 処理概要 | 遷移先 / 結果 | 実行可能ロール |
|----------|---------|---------|-------------|-------------|
| `EVT-INB002-001` | 画面初期表示 | 入荷予定日に現在営業日をセット。仕入先プルダウンリストを取得 | — | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB002-002` | 商品コード入力（blur / Enter） | 商品マスタ検索API呼び出し。ヒット時に商品名を自動補完。未ヒット時はエラーメッセージ表示 | 同行の商品名欄を更新 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB002-003` | 「＋ 行追加」ボタンクリック | 明細テーブル末尾に空行を追加 | 同画面 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB002-004` | 「削除（×）」ボタンクリック | 対象行を削除。行が1行のみの場合は操作不可 | 同画面 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB002-005` | 「キャンセル」ボタンクリック | 未保存データあり時は確認ダイアログを表示。OKでINB-001へ遷移 | INB-001 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB002-006` | 「登録する」ボタンクリック | フォーム全体のバリデーション実行。OK後 POST /api/v1/inbound/slips を呼び出し。成功でINB-003（登録した伝票の詳細）へ遷移し成功メッセージ表示 | INB-003 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |

---

### 5. メッセージ一覧

| メッセージID | 種別 | 発生条件 | メッセージ文（日本語） |
|------------|------|---------|-------------------|
| `MSG-E-101` | エラー | 入荷予定日が未入力 | 入荷予定日は必須です |
| `MSG-E-102` | エラー | 入荷予定日が現在営業日より前の日付 | 入荷予定日は現在営業日以降の日付を入力してください |
| `MSG-E-103` | エラー | 仕入先が未選択 | 仕入先は必須です |
| `MSG-E-104` | エラー | 商品コードが商品マスタに存在しない | 商品コード「{code}」は存在しません |
| `MSG-E-105` | エラー | 予定数量が未入力または0以下 | 予定数量は1以上の整数を入力してください |
| `MSG-E-106` | エラー | 同一伝票内に商品コードの重複あり | 同一商品が複数明細に含まれています。明細を統合してください |
| `MSG-E-107` | エラー | 明細が0行 | 少なくとも1件の入荷明細を入力してください |
| `MSG-W-101` | 警告 | キャンセルボタンクリック（未保存データあり） | 入力内容が破棄されます。よろしいですか？ |
| `MSG-S-101` | 成功 | 登録成功 | 入荷予定を登録しました（伝票番号: {slipNo}） |

---
---

## INB-003 入荷予定詳細

### 1. 画面概要

| 項目 | 内容 |
|------|------|
| **画面ID** | `INB-003` |
| **画面名** | 入荷予定詳細 |
| **URL パス** | `/inbound/slips/:id` |
| **対象ロール** | WAREHOUSE_MANAGER, WAREHOUSE_STAFF, VIEWER |
| **概要** | 入荷予定の登録内容・ステータス・明細を参照する画面。ステータスに応じて「入荷確認」「入荷キャンセル」「検品へ」「入庫確定へ」等の操作ボタンを表示する。VIEWERは参照のみ可能。 |

---

### 2. 画面レイアウト

**モックアップ**: [INB-003-inbound-detail.html](mockups/INB-003-inbound-detail.html)

```
┌─────────────────────────────────────────────────────────────┐
│ ヘッダー                                                     │
├──────────┬──────────────────────────────────────────────────┤
│ サイドバー│ ホーム › 入荷管理 › 入荷予定一覧 › 詳細         │
│          │────────────────────────────────────────────────│
│          │ 入荷予定詳細     ステータス: [入荷確認済 badge]   │
│          │                                                  │
│          │ ┌──────────────────────────────────────────┐   │
│          │ │ 基本情報                                  │   │
│          │ │  伝票番号     INB-20260313-001             │   │
│          │ │  種別         通常入荷                     │   │
│          │ │  入荷予定日   2026/03/20                   │   │
│          │ │  仕入先       株式会社ABC商事               │   │
│          │ │  備考         春季補充分                   │   │
│          │ │  登録日時     2026/03/13 10:30             │   │
│          │ │  登録者       山田 太郎                    │   │
│          │ └──────────────────────────────────────────┘   │
│          │ ┌──────────────────────────────────────────┐   │
│          │ │ 入荷明細                                  │   │
│          │ │ No │ 商品コード │ 商品名 │予定数│検品数│差異│  │
│          │ │  1 │ PRD-0001  │ XXX   │  100 │  — │  — │  │
│          │ └──────────────────────────────────────────┘   │
│          │                                                  │
│          │ [← 一覧に戻る]  [キャンセル] [入荷確認] [検品へ→]│
└──────────┴──────────────────────────────────────────────────┘
```

コンテンツエリアの構成:
1. **ページタイトル右にステータスバッジ**: 現在のステータスをカラーバッジで表示
2. **基本情報カード**: 伝票番号・種別・入荷予定日・仕入先・備考・登録日時・登録者（すべて読み取り専用）
3. **入荷明細テーブル**: No・商品コード・商品名・予定数量・検品数・差異数（検品前は「—」表示）
4. **操作ボタンエリア**: ステータスに応じてボタンを動的に表示（下記イベント一覧参照）

---

### 3. 画面項目一覧

| 項目ID | 項目名 | 種別 | 必須 | 初期値 | バリデーション | 備考 |
|--------|--------|------|:----:|--------|--------------|------|
| INB003-H01 | 伝票番号 | 表示 | — | URLパラメータから取得 | — | |
| INB003-H02 | ステータスバッジ | 表示 | — | API取得値 | — | ステータス別カラー表示 |
| INB003-H03 | 種別 | 表示 | — | API取得値 | — | |
| INB003-H04 | 入荷予定日 | 表示 | — | API取得値 | — | |
| INB003-H05 | 仕入先コード | 表示 | — | API取得値 | — | |
| INB003-H06 | 仕入先名 | 表示 | — | API取得値 | — | |
| INB003-H07 | 備考 | 表示 | — | API取得値 | — | |
| INB003-H08 | 登録日時 | 表示 | — | API取得値 | — | |
| INB003-H09 | 登録者 | 表示 | — | API取得値 | — | |
| INB003-D01 | 明細No | 表示 | — | — | — | |
| INB003-D02 | 商品コード | 表示 | — | — | — | |
| INB003-D03 | 商品名 | 表示 | — | — | — | |
| INB003-D04 | 予定数量 | 表示 | — | — | — | |
| INB003-D05 | 検品数 | 表示 | — | — | — | 検品前は「—」表示 |
| INB003-D06 | 差異数 | 表示 | — | — | — | 検品前は「—」。差異あり時は赤字 |
| INB003-BTN01 | 一覧に戻るボタン | ボタン | — | — | — | INB-001へ遷移 |
| INB003-BTN02 | 入荷確認ボタン | ボタン | — | — | — | ステータス「入荷予定」時のみ表示。VIEWER非表示 |
| INB003-BTN03 | キャンセルボタン | ボタン | — | — | — | ステータス「入庫完了」「キャンセル」以外で表示。VIEWER非表示 |
| INB003-BTN04 | 検品へボタン | ボタン | — | — | — | ステータス「入荷確認済」「検品中」時に表示。VIEWER非表示 |
| INB003-BTN05 | 入庫確定へボタン | ボタン | — | — | — | ステータス「検品中」「一部入庫」時に表示。VIEWER非表示 |
| INB003-BTN06 | 検品レポートボタン | ボタン | — | — | — | 常時表示 |

---

### 4. イベント一覧

| イベントID | トリガー | 処理概要 | 遷移先 / 結果 | 実行可能ロール |
|----------|---------|---------|-------------|-------------|
| `EVT-INB003-001` | 画面初期表示 | URLパラメータ(:id)でGET /api/v1/inbound/slips/:id を呼び出し、伝票情報・明細を取得して表示 | — | 全対象ロール |
| `EVT-INB003-002` | 「入荷確認」ボタンクリック | 確認ダイアログ表示。OK後 PATCH /api/v1/inbound/slips/:id/confirm を呼び出し。成功でステータス更新・成功メッセージ表示 | 同画面リロード | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB003-003` | 「キャンセル」ボタンクリック | 確認ダイアログ表示（キャンセル不可の旨を警告）。OK後 PATCH /api/v1/inbound/slips/:id/cancel を呼び出し | 同画面リロード | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB003-004` | 「検品へ」ボタンクリック | INB-004へ遷移 | INB-004 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB003-005` | 「入庫確定へ」ボタンクリック | INB-005へ遷移 | INB-005 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB003-006` | 「← 一覧に戻る」クリック | INB-001へ遷移 | INB-001 | 全対象ロール |
| `EVT-INB003-007` | 「検品レポート」ボタンクリック | RPT-001（入荷検品レポート出力ダイアログ）を表示 | ダイアログ表示 | 全対象ロール |

---

### 5. メッセージ一覧

| メッセージID | 種別 | 発生条件 | メッセージ文（日本語） |
|------------|------|---------|-------------------|
| `MSG-W-102` | 警告 | 入荷確認ボタンクリック | 入荷確認します。よろしいですか？ |
| `MSG-W-103` | 警告 | キャンセルボタンクリック | 入荷をキャンセルします。キャンセル後は修正できません。よろしいですか？ |
| `MSG-S-102` | 成功 | 入荷確認成功 | 入荷確認しました |
| `MSG-S-103` | 成功 | キャンセル成功 | 入荷をキャンセルしました |
| `MSG-E-108` | エラー | API呼び出しエラー（404） | 指定の入荷伝票が見つかりません |
| `MSG-E-109` | エラー | キャンセル時に既に入庫完了 | 入庫完了済みのためキャンセルできません |

---
---

## INB-004 入荷検品

### 1. 画面概要

| 項目 | 内容 |
|------|------|
| **画面ID** | `INB-004` |
| **画面名** | 入荷検品 |
| **URL パス** | `/inbound/slips/:id/inspect` |
| **対象ロール** | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| **概要** | 到着した荷物を明細単位で検品し、実際の入荷数を入力・確定する画面。入荷数の初期値は入荷予定数と同数にプリセットされており、差異がある明細のみ修正して確定できる。最初の保存操作でステータスが「検品中」に遷移する。 |

---

### 2. 画面レイアウト

**モックアップ**: [INB-004-inbound-inspect.html](mockups/INB-004-inbound-inspect.html)

```
┌─────────────────────────────────────────────────────────────┐
│ ヘッダー                                                     │
├──────────┬──────────────────────────────────────────────────┤
│ サイドバー│ ホーム › 入荷管理 › 詳細 › 入荷検品             │
│          │────────────────────────────────────────────────│
│          │ 入荷検品                                         │
│          │                                                  │
│          │ ┌──────── 伝票情報 ────────────────────────┐  │
│          │ │ 伝票番号: INB-20260313-001   仕入先: ABC商事│  │
│          │ │ 入荷予定日: 2026/03/20       ステータス: 検品中│  │
│          │ └────────────────────────────────────────────┘  │
│          │                                                  │
│          │ ┌──────── 入荷明細 ─────────────────────────┐ │
│          │ │ No │商品コード│商品名│予定数│入荷数（入力）│差異│ │
│          │ │  1 │PRD-0001 │XXX  │ 100 │[  100  ]   │  0 │ │
│          │ │  2 │PRD-0002 │YYY  │  50 │[   48  ]   │ -2 │ │
│          │ └────────────────────────────────────────────┘ │
│          │                                                  │
│          │            [← 詳細に戻る]  [検品内容を保存する]  │
└──────────┴──────────────────────────────────────────────────┘
```

コンテンツエリアの構成:
1. **伝票情報サマリーバー**: 伝票番号・仕入先・入荷予定日・ステータスを1行でコンパクト表示
2. **検品明細テーブル**: No・商品コード・商品名・予定数量（読み取り専用）・入荷数（入力欄・初期値は予定数量）・差異数（リアルタイム計算・差異あり時は赤字）
3. **フッターボタン**: 「← 詳細に戻る」「検品内容を保存する」

差異数 = 入荷数 - 予定数量。入荷数変更時にリアルタイムで再計算して表示する。

---

### 3. 画面項目一覧

| 項目ID | 項目名 | 種別 | 必須 | 初期値 | バリデーション | 備考 |
|--------|--------|------|:----:|--------|--------------|------|
| INB004-S01 | 伝票番号 | 表示 | — | URLパラメータから取得 | — | |
| INB004-S02 | 仕入先名 | 表示 | — | API取得値 | — | |
| INB004-S03 | 入荷予定日 | 表示 | — | API取得値 | — | |
| INB004-S04 | ステータス | 表示 | — | API取得値 | — | バッジ表示 |
| INB004-D01 | 明細No | 表示 | — | — | — | |
| INB004-D02 | 商品コード | 表示 | — | — | — | |
| INB004-D03 | 商品名 | 表示 | — | — | — | |
| INB004-D04 | 予定数量 | 表示 | — | API取得値 | — | 読み取り専用 |
| INB004-D05 | 入荷数 | 数値 | ○ | 予定数量と同値 | 必須・0以上999999以下の整数 | 変更時に差異数をリアルタイム更新 |
| INB004-D06 | 差異数 | 表示 | — | 0（予定数量と同値のため） | — | 差異数=(入荷数-予定数量)。差異あり時は赤字 |
| INB004-BTN01 | 詳細に戻るボタン | ボタン | — | — | — | INB-003へ遷移 |
| INB004-BTN02 | 検品内容を保存するボタン | ボタン | — | — | — | PUT /api/v1/inbound/slips/:id/inspect を呼び出し |

---

### 4. イベント一覧

| イベントID | トリガー | 処理概要 | 遷移先 / 結果 | 実行可能ロール |
|----------|---------|---------|-------------|-------------|
| `EVT-INB004-001` | 画面初期表示 | GET /api/v1/inbound/slips/:id で伝票・明細情報を取得。各明細の入荷数に予定数量をプリセット | — | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB004-002` | 入荷数（数値）変更 | 差異数（=入荷数 - 予定数量）をリアルタイム計算して差異数セルを更新。差異あり行は行背景を淡黄色にハイライト | 同行差異数更新 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB004-003` | 「検品内容を保存する」ボタンクリック | 入荷数0以上のバリデーション実行。OK後 PUT /api/v1/inbound/slips/:id/inspect を呼び出し。初回保存時はステータスが「検品中」に遷移。成功後INB-003へ遷移し成功メッセージ表示 | INB-003 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB004-004` | 「← 詳細に戻る」クリック | INB-003へ遷移 | INB-003 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |

---

### 5. メッセージ一覧

| メッセージID | 種別 | 発生条件 | メッセージ文（日本語） |
|------------|------|---------|-------------------|
| `MSG-E-110` | エラー | 入荷数に負の値が入力された | 入荷数は0以上の整数を入力してください |
| `MSG-E-111` | エラー | 入荷数が未入力 | 入荷数は必須です |
| `MSG-I-101` | 情報 | 全明細の差異数が0の場合の保存前案内 | 全明細が予定数通りです。このまま確定しますか？ |
| `MSG-S-104` | 成功 | 検品内容保存成功 | 検品内容を保存しました |
| `MSG-W-104` | 警告 | 差異がある明細が1件以上ある場合の保存確認 | {N}件の明細に差異があります。このまま確定しますか？ |

---
---

## INB-005 入庫指示・確定

### 1. 画面概要

| 項目 | 内容 |
|------|------|
| **画面ID** | `INB-005` |
| **画面名** | 入庫指示・確定 |
| **URL パス** | `/inbound/slips/:id/store` |
| **対象ロール** | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| **概要** | 検品済みの入荷商品を倉庫内のロケーションへ格納する指示を確定する画面。システムが自動割当した候補ロケーションが初期表示され、ユーザーは変更するか確認してから明細ごと（または全明細一括）に入庫確定を実行する。 |

---

### 2. 画面レイアウト

**モックアップ**: [INB-005-inbound-store.html](mockups/INB-005-inbound-store.html)

```
┌─────────────────────────────────────────────────────────────┐
│ ヘッダー                                                     │
├──────────┬──────────────────────────────────────────────────┤
│ サイドバー│ ホーム › 入荷管理 › 詳細 › 入庫指示・確定       │
│          │────────────────────────────────────────────────│
│          │ 入庫指示・確定                                   │
│          │                                                  │
│          │ ┌───── 伝票情報 ────────────────────────────┐  │
│          │ │ 伝票番号: INB-20260313-001  仕入先: ABC商事 │  │
│          │ └────────────────────────────────────────────┘  │
│          │                                                  │
│          │ ┌───── 入庫明細 ─────────────────────────────┐ │
│          │ │[全件入庫確定]                               │ │
│          │ │No│商品名│検品数│入庫先ロケーション│入庫状態│操作│ │
│          │ │ 1│XXX   │  100│[A-01-01 ▼]     │未入庫  │[確定]││
│          │ │ 2│YYY   │   48│[B-02-03 ▼]     │入庫済  │[済] │ │
│          │ └────────────────────────────────────────────┘ │
│          │                                                  │
│          │              [← 詳細に戻る]                     │
└──────────┴──────────────────────────────────────────────────┘
```

コンテンツエリアの構成:
1. **伝票情報サマリーバー**: 伝票番号・仕入先・ステータス
2. **入庫明細テーブル**: No・商品コード・商品名・検品数（読み取り専用）・入庫先ロケーション（セレクト・自動割当候補がプリセット）・入庫状態（未入庫/入庫済）・操作（確定ボタン or 済バッジ）
3. **全件入庫確定ボタン**: テーブル上部に配置。未入庫の全明細を一括確定
4. **フッターボタン**: 「← 詳細に戻る」

---

### 3. 画面項目一覧

| 項目ID | 項目名 | 種別 | 必須 | 初期値 | バリデーション | 備考 |
|--------|--------|------|:----:|--------|--------------|------|
| INB005-S01 | 伝票番号 | 表示 | — | URLパラメータから取得 | — | |
| INB005-S02 | 仕入先名 | 表示 | — | API取得値 | — | |
| INB005-S03 | ステータスバッジ | 表示 | — | API取得値 | — | |
| INB005-D01 | 明細No | 表示 | — | — | — | |
| INB005-D02 | 商品コード | 表示 | — | — | — | |
| INB005-D03 | 商品名 | 表示 | — | — | — | |
| INB005-D04 | 検品数 | 表示 | — | API取得値 | — | 読み取り専用 |
| INB005-D05 | 入庫先ロケーション | セレクト | ○ | システム自動割当候補 | 必須（確定時） | 有効ロケーション一覧から選択可能。入庫済み行は読み取り専用 |
| INB005-D06 | 入庫状態 | 表示 | — | 未入庫 or 入庫済 | — | 入庫済は緑バッジ |
| INB005-D07 | 自動割当根拠 | 表示 | — | 「既存在庫あり」or「空きロケーション」 | — | ロケーション選択欄の補助テキストとして表示 |
| INB005-BTN01 | 全件入庫確定ボタン | ボタン | — | — | — | 未入庫の全明細を一括確定。全件入庫済の場合は非活性 |
| INB005-BTN02 | 個別確定ボタン | ボタン | — | — | — | 明細行ごとに表示。入庫済行は非活性 |
| INB005-BTN03 | 詳細に戻るボタン | ボタン | — | — | — | INB-003へ遷移 |

---

### 4. イベント一覧

| イベントID | トリガー | 処理概要 | 遷移先 / 結果 | 実行可能ロール |
|----------|---------|---------|-------------|-------------|
| `EVT-INB005-001` | 画面初期表示 | GET /api/v1/inbound/slips/:id/store-instructions で伝票・検品済み明細・自動割当候補ロケーションを取得して表示 | — | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB005-002` | 個別「確定」ボタンクリック | ロケーション選択必須チェック後、POST /api/v1/inbound/slips/:id/store/{lineNo} を呼び出し。一部入庫時はステータスが「一部入庫」に遷移。成功で当該行の入庫状態を「入庫済」に更新 | 同画面更新 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB005-003` | 「全件入庫確定」ボタンクリック | 未入庫全明細のロケーション選択必須チェック後、確認ダイアログ表示。OK後 POST /api/v1/inbound/slips/:id/store-all を呼び出し。全件完了でステータスが「入庫完了」に遷移。成功でINB-003へ遷移 | INB-003 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |
| `EVT-INB005-004` | 「← 詳細に戻る」クリック | INB-003へ遷移 | INB-003 | WAREHOUSE_MANAGER, WAREHOUSE_STAFF |

---

### 5. メッセージ一覧

| メッセージID | 種別 | 発生条件 | メッセージ文（日本語） |
|------------|------|---------|-------------------|
| `MSG-E-112` | エラー | ロケーションが未選択の状態で確定操作 | 入庫先ロケーションを選択してください |
| `MSG-W-105` | 警告 | 全件入庫確定ボタンクリック | 未入庫の全明細を入庫確定します。よろしいですか？ |
| `MSG-S-105` | 成功 | 個別入庫確定成功 | {商品名}を入庫確定しました（ロケーション: {location}） |
| `MSG-S-106` | 成功 | 全件入庫確定成功 | 全明細の入庫確定が完了しました。ステータスが「入庫完了」になりました |
| `MSG-I-102` | 情報 | 自動割当候補が見つからない明細がある場合 | {N}件の明細について自動割当候補が見つかりませんでした。手動でロケーションを選択してください |

---
---

## INB-006 入荷実績照会

### 1. 画面概要

| 項目 | 内容 |
|------|------|
| **画面ID** | `INB-006` |
| **画面名** | 入荷実績照会 |
| **URL パス** | `/inbound/results` |
| **対象ロール** | WAREHOUSE_MANAGER, WAREHOUSE_STAFF, VIEWER |
| **概要** | 入庫完了した入荷実績を照会する画面。入荷予定数・検品数・差異数を明細レベルで確認でき、入荷日・仕入先・商品コード等で絞り込みが可能。入庫実績レポートのPDF出力もサポートする。 |

---

### 2. 画面レイアウト

**モックアップ**: [INB-006-inbound-results.html](mockups/INB-006-inbound-results.html)

```
┌─────────────────────────────────────────────────────────────┐
│ ヘッダー                                                     │
├──────────┬──────────────────────────────────────────────────┤
│ サイドバー│ ホーム › 入荷管理 › 入荷実績照会                │
│          │────────────────────────────────────────────────│
│          │ 入荷実績照会                                     │
│          │                                                  │
│          │ ┌───── 絞り込み ────────────────────────────┐  │
│          │ │入庫日(From)[____] (To)[____] 仕入先[____▼] │  │
│          │ │伝票番号[____________] 商品コード[__________]│  │
│          │ │            [🔍 検索]  [クリア]              │  │
│          │ └────────────────────────────────────────────┘  │
│          │                                                  │
│          │  [📄 入庫実績レポート出力]        全 42件        │
│          │ ┌─────────────────────────────────────────────┐ │
│          │ │伝票番号│入庫日│仕入先│商品名│予定数│検品数│差異│ │
│          │ │INB-001 │03/13 │ABC │XXX  │ 100 │  98 │ -2 │ │
│          │ └─────────────────────────────────────────────┘ │
│          │                     ← 1 2 3 → 20件/ページ▼      │
└──────────┴──────────────────────────────────────────────────┘
```

コンテンツエリアの構成:
1. **絞り込みフォーム**: 入庫日From/To・仕入先・伝票番号・商品コードで絞り込み
2. **ツールバー**: 件数表示・入庫実績レポート出力ボタン
3. **実績テーブル**: 伝票番号・入庫完了日・仕入先・商品コード・商品名・予定数量・検品数・差異数を表示。差異数が0以外の行は差異数を赤字表示
4. **ページネーション**: 20件/ページ（変更可）

---

### 3. 画面項目一覧

| 項目ID | 項目名 | 種別 | 必須 | 初期値 | バリデーション | 備考 |
|--------|--------|------|:----:|--------|--------------|------|
| INB006-F01 | 入庫日（From） | 日付 | — | 現在営業日の月初（1日） | FromはTo以前 | |
| INB006-F02 | 入庫日（To） | 日付 | — | 現在営業日 | ToはFrom以降 | |
| INB006-F03 | 仕入先 | セレクト | — | — | — | 「すべて」選択可 |
| INB006-F04 | 伝票番号 | テキスト | — | — | 最大30文字 | 前方一致検索 |
| INB006-F05 | 商品コード | テキスト | — | — | 最大30文字 | 前方一致検索 |
| INB006-BTN01 | 検索ボタン | ボタン | — | — | — | GET /api/v1/inbound/results を呼び出し |
| INB006-BTN02 | クリアボタン | ボタン | — | — | — | 絞り込み条件をリセットして再検索 |
| INB006-BTN03 | 入庫実績レポート出力ボタン | ボタン | — | — | — | RPT-004を呼び出し |
| INB006-T01 | 伝票番号 | 表示 | — | — | — | クリックでINB-003へ遷移 |
| INB006-T02 | 入庫完了日 | 表示 | — | — | — | |
| INB006-T03 | 仕入先名 | 表示 | — | — | — | |
| INB006-T04 | 商品コード | 表示 | — | — | — | |
| INB006-T05 | 商品名 | 表示 | — | — | — | |
| INB006-T06 | 予定数量 | 表示 | — | — | — | 右寄せ |
| INB006-T07 | 検品数 | 表示 | — | — | — | 右寄せ |
| INB006-T08 | 差異数 | 表示 | — | — | — | 右寄せ。0以外は赤字で表示 |
| INB006-P01 | ページネーション | ページング | — | 1ページ目 | — | 20/50/100件切替可 |

---

### 4. イベント一覧

| イベントID | トリガー | 処理概要 | 遷移先 / 結果 | 実行可能ロール |
|----------|---------|---------|-------------|-------------|
| `EVT-INB006-001` | 画面初期表示 | 入庫日デフォルト（現在営業日の月初〜現在営業日）でGET /api/v1/inbound/results を呼び出し、結果を表示 | — | 全対象ロール |
| `EVT-INB006-002` | 「検索」ボタンクリック | 入力条件でGET /api/v1/inbound/results を呼び出し。日付範囲バリデーション後に検索実行 | 同画面リロード（結果反映） | 全対象ロール |
| `EVT-INB006-003` | 「クリア」ボタンクリック | 絞り込み条件を初期値にリセットして再検索 | 同画面リロード | 全対象ロール |
| `EVT-INB006-004` | テーブルの伝票番号リンククリック | INB-003（当該伝票の詳細）へ遷移 | INB-003 | 全対象ロール |
| `EVT-INB006-005` | 「入庫実績レポート出力」ボタンクリック | 現在の絞り込み条件を引き継いでRPT-004ダイアログを表示 | ダイアログ表示 | 全対象ロール |
| `EVT-INB006-006` | ページネーション操作 | 指定ページのデータを再取得して表示 | 同画面リロード（ページ更新） | 全対象ロール |

---

### 5. メッセージ一覧

| メッセージID | 種別 | 発生条件 | メッセージ文（日本語） |
|------------|------|---------|-------------------|
| `MSG-E-113` | エラー | 入庫日FromがToより後の日付 | 入庫日の開始日は終了日以前の日付を入力してください |
| `MSG-I-103` | 情報 | 検索結果が0件 | 条件に一致する入荷実績が見つかりませんでした |
